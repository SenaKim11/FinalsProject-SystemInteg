// Add click event listeners to the buttons
const xButton = document.querySelector('.x');
const heartButton = document.querySelector('.heart');
const hamburgerButton = document.querySelector('.hamburger');
const starButton = document.querySelector('.star');

xButton.addEventListener('click', () => {
  // Do something when the X button is clicked
});

heartButton.addEventListener('click', () => {
  // Do something when the Heart button is clicked
});

hamburgerButton.addEventListener('click', () => {
  // Do something when the Hamburger button is clicked
});

starButton.addEventListener('click', () => {
  // Do something when the Star button is clicked
});
